import { View, Text, Modal } from "react-native";
import React from "react";

const NoteInputModal = () => {
  return (
    <Modal>
      <Text>NoteInputModal</Text>
    </Modal>
  );
};

export default NoteInputModal;
